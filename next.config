/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // If you need to configure additional settings (like images or environment variables), you can add them here.
};

module.exports = nextConfig;
